<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <!-- Bootstrap 5 CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .dashboard-card {
            border-radius: 10px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease-in-out;
        }
        .dashboard-card:hover {
            transform: translateY(-5px);
        }
    </style>
</head>
<body class="bg-light">
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="#">My Dashboard</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link text-white" href="/edit-profile">Edit Profile</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-white" href="/logout">Logout</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Dashboard Content -->
    <div class="container mt-5">
        <h2 class="text-center text-primary">Welcome, <%= user.name %>!</h2>
        <p class="text-center text-muted">Here is an overview of your account</p>

        <div class="row mt-4">
            <!-- Profile Card -->
            <div class="col-md-4">
                <div class="card dashboard-card p-3">
                    <h5 class="text-primary">Your Profile</h5>
                    <p><strong>Name:</strong> <%= user.name %></p>
                    <p><strong>Email:</strong> <%= user.email %></p>
                    <p><strong>Phone:</strong> <%= user.phone %></p>
                    <p><strong>City:</strong> <%= user.city %></p>
                    <p><strong>Gender:</strong> <%= user.gender %></p>
                    <a href="/edit-profile" class="btn btn-sm btn-primary">Edit Profile</a>
                </div>
            </div>

            <!-- Statistics Card -->
            <div class="col-md-4">
                <div class="card dashboard-card p-3">
                    <h5 class="text-success">Statistics</h5>
                    <p><strong>Total Logins:</strong> 15</p>
                    <p><strong>Last Login:</strong> 2025-03-20</p>
                    <p><strong>Account Created:</strong> 2025-01-10</p>
                </div>
            </div>

            <!-- Actions Card -->
            <div class="col-md-4">
                <div class="card dashboard-card p-3">
                    <h5 class="text-danger">Quick Actions</h5>
                    <a href="/settings" class="btn btn-sm btn-secondary mb-2">Settings</a>
                    <a href="/support" class="btn btn-sm btn-info mb-2">Support</a>
                    <a href="/logout" class="btn btn-sm btn-danger">Logout</a>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
